spring:
  r2dbc:
    url: r2dbc:h2:mem:test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    properties:
      schema: kss
  flyway:
    url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    user: sa
    password:
    default-schema: kss
    enabled: false